# works only for piholes ver. 6+
pi-holes:
    - url: 'http://192.168.1.3:9080'
      password: '******'
      name: 'backup-pihole'
    - url: 'http://192.168.1.4:9080'
      password: '******'
      name: 'primary-pihole'

# uncomment following section to use bitwarden
# NOTE: Any item must have defined custom field
# named `active` of type `checkbox` and set to `true` to be used.
# search is case-insensitive and will search in all bitwarden items
# that contains (like) search-string in their name
#pi-holes-from-bitwarden:
#  search-string: 'pihole'

allowed-domains:
    - domain: 'www.googleadservices.com'
      type: 'exact-allow'
    - domain: 'click.discord.com'
      type: 'exact-allow'
    - domain: 'tracking.mojebohema.cz'
      type: 'exact-allow'
    - domain: 'click.rpishop.cz'
      type: 'exact-allow'
    - domain: 'www.google.com'
      type: 'exact-allow'

local-dns-records:
    - domain: 'home.lab'
      ip: '192.168.1.5'
